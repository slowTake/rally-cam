defaults:
        - _self_
        - global
        - runner: inference
        - model: deepball
        - dataset: tabletennis
        - dataloader: default
        - detector: deepball
        - transform: default
        - tracker: intra_frame_peak
detector:
  postprocessor:
    use_hm_weight: False
  step: 1
hydra:
  run:
    dir: ${WASB_ROOT}/outputs/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}
output_dir:
seed: 1234
